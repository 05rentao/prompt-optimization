# Optional API configuration
OPENAI_API_KEY=

# Optional model overrides for OpenAI backend mode
OPENAI_TASK_MODEL=gpt-4o-mini
OPENAI_EVAL_MODEL=gpt-4o-mini

# Optional Hugging Face token when needed
HF_TOKEN=
