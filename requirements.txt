# Core
torch>=2.0
transformers>=4.36
accelerate>=0.25
bitsandbytes>=0.41
datasets>=2.14
tqdm>=4.65
pyyaml>=6.0

# Adversary: LoRA via PEFT (RedTeamPolicy); no Unsloth needed, backward works every batch
peft>=0.10

# GEPA defender (optional): when defense.USE_GEPA_DEFENDER is true, BlueTeam uses this
dspy-ai

# Judge: cais/HarmBench-Llama-2-13b-cls (4-bit, bfloat16) via transformers + bitsandbytes.
# No OpenAI key. GPU with ~8–10 GB+ VRAM recommended for the 13B judge.
# GEPA: if enabled, requires Ollama with llama3:8b (ollama pull llama3:8b).
