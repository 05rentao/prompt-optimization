# Core
torch>=2.0
transformers>=4.36
accelerate>=0.25
bitsandbytes>=0.41
datasets>=2.14
tqdm>=4.65
pyyaml>=6.0

# Adversary: 4-bit + LoRA (required for RedTeamPolicy)
unsloth

# Note: Judge uses cais/HarmBench-Llama-2-13b-cls (4-bit, bfloat16) via transformers + bitsandbytes.
# No OpenAI or other API key required. GPU with ~8–10 GB+ VRAM recommended for the 13B judge.
