# `src/` Guide Moved

The `src/README_guide.md` content has been merged into `src/README.md`.

Use `src/README.md` as the single source of truth for:
- module layout and runtime architecture
- teammate quickstart
- step-by-step guide for creating new run scripts
- script templates and extension conventions