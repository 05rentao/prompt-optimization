"""
Package placeholder for uv/pyproject build.

The distribution name `stat4830project-folder` is normalized to the
Python package `stat4830project_folder`, so this empty package satisfies
the build backend without affecting runtime code in `src/`.
"""

